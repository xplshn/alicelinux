name=l3afpad
version=0.8.18.1.11
release=1
source="https://github.com/stevenhoneyman/${name}/archive/v${version}/${name}-v${version}.tar.gz"

prebuild() {
	./autogen.sh
}

postbuild() {
	ln -s l3afpad $PKG/usr/bin/leafpad
}
