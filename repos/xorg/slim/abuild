name=slim
version=1.4.0
release=1
source="https://sourceforge.net/projects/$name-fork/files/$name-$version.tar.gz
	slim.conf
	run"
sv="run"
build_opt="
	-DUSE_CONSOLEKIT=no \
	-DUSE_PAM=no
"

postbuild() {
	install -m644 $SRC/slim.conf $PKG/etc/slim.conf
}
