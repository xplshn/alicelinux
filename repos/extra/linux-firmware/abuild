name=linux-firmware
version=20250808
release=1
source="https://www.kernel.org/pub/linux/kernel/firmware/${name}-${version}.tar.xz
	dedup-firmware.sh
	copy-firmware.sh"

build() {
	cp $SRC/*-firmware.sh .
	chmod +x *.sh
	make install
	make dedup
}
