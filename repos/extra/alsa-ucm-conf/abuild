name=alsa-ucm-conf
version=1.2.9
release=1
source="https://www.alsa-project.org/files/pub/lib/${name}-${version}.tar.bz2"

build() {
	install -vdm 755 "$PKG/usr/share/alsa/"
	cp -av ucm2 "$PKG/usr/share/alsa/"
}

